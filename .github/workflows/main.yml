name: Testing

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - testing*
jobs:
  testing:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
#       - name: update env
#         run: |
#           mkdir -p /home/runner/bin
#           echo 'echo "Print the PATH: $PATH"' >> printpath.sh
#           chmod +x printpath.sh
#           cp printpath.sh /home/runner/bin/
#           echo "PATH=$PATH:/home/runner/bin" >>  ${GITHUB_ENV}
#       - name: Check if env var is updated
#         run: |
#           echo $PATH
#           printpath.sh
      - name: test2
        run: |
          mkdir -p ${GITHUB_WORKSPACE}
          curl -Lo ${GITHUB_WORKSPACE}/kubectl https://dl.k8s.io/release/v1.27.1/bin/linux/amd64/kubectl
      - name: test3
        run: |
          echo ${GITHUB_WORKSPACE}/
          ls -l ${GITHUB_WORKSPACE}/

